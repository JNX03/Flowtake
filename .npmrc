# eslint 10 is ahead of eslint-plugin-react's declared peer range (<=9.7),
# which makes strict `npm ci` abort with ERESOLVE. The plugin works fine at
# runtime, so relax peer resolution repo-wide. This keeps `npm ci` (CI/release),
# local installs, and Dependabot lockfile updates consistent.
legacy-peer-deps=true
