{
  "build": {
    "beforeDevCommand": "npm run build:macos-capture && npm run dev:frontend",
    "beforeBuildCommand": "npm run build:macos-capture && npm run build:frontend"
  },
  "bundle": {
    "externalBin": [
      "binaries/ffmpeg",
      "binaries/flowtake-macos-capture"
    ]
  }
}
